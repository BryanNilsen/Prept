import React from "react";
import ReactDOM from "react-dom";
import Prept from "./Prept";
import { BrowserRouter as Router } from "react-router-dom";

ReactDOM.render(
  <Router>
    <Prept />
  </Router>,
  document.getElementById("root")
);
